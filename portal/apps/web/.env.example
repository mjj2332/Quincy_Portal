# Quincy Portal — web app environment.
#
# Copy the entries you need into `.env.local`, which is gitignored. `.env.local` must sit HERE,
# beside `components.json`, not at the repo root: the shadcn CLI looks for it in its own working
# directory. This file carries NO values that are secret — never copy a real key back into it.

# ---------------------------------------------------------------------------
# ReUI component registry
# ---------------------------------------------------------------------------
# The licence key for the registry in `components.json`, which refers to it as
# ${REUI_LICENSE_KEY} and expands it. Never inline the key into `components.json` itself.
# `src/config/reui-registry.guard.test.ts` fails the build if either rule is broken.
REUI_LICENSE_KEY=

# ---------------------------------------------------------------------------
# Feature flags
# ---------------------------------------------------------------------------
# Every flag is BUILD-TIME: Vite substitutes `import.meta.env.VITE_*` at build, so flipping one
# needs a rebuild, not a reload. Each is read in exactly one place, through a pure predicate in
# `src/lib/feature-flags.ts`.
#
# The value must be exactly the string `1`. Anything else is off — including `true`, `yes` and
# `on`. This is deliberate: env values are strings, so every one of those is truthy in JS, and a
# flag that switched ON for someone writing `=false` would be worse than no flag at all.

# The left navigation rail (#111), in place of the Topbar. Unset by default.
#
# KNOWN PARITY GAP while this is on: the rail carries navigation, Admin, the signed-in identity and
# Sign out, but it has NO notification bell and NO link to notification preferences — both are
# Topbar-only until #113 builds the notification centre into the rail. So a session with the rail
# switched on cannot reach notifications at all. Expected, and the reason the Topbar is still what
# ships by default.
VITE_QUINCY_NAV_RAIL=
