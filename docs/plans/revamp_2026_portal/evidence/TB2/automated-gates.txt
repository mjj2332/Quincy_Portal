# TB2 automated gates and source audits

Date: 2026-08-25. No manual QA, screenshots, sign-in, production interaction, deployment, or plan status update was performed.

## Focused TB2 suites

`npx vitest run --config apps/web/vitest.config.ts src/lib/project-data.test.ts src/lib/project-query-sync.test.ts`

- PASS — 2 files, 19 tests.

`npx vitest run --config apps/web/vitest.dom.config.ts src/screens/ProjectWorkspace.dom.test.tsx src/App.dom.test.tsx src/components/Lightbox.dom.test.tsx`

- PASS — 3 files, 49 tests.

## Repository-standard automated gate

`npm run typecheck`

- PASS — all workspace typecheck scripts completed.

`npm run build -w @quincy/web`

- PASS — Vite production build; 303 modules transformed.
- The expected Vite warning remains: the existing single JS chunk is larger than 500 kB.

`npm run test --workspaces`

- EXPECTED NONZERO EXIT — npm first reports the documented `@quincy/shared` missing `test` script, then runs the other workspaces.
- EXPECTED SANDBOX NONZERO EXIT after the documented `@quincy/shared` missing `test` script: `@quincy/db` passed 8 files / 22 tests; `@quincy/web` passed 13 node files / 88 tests and 22 DOM files / 216 tests; `@quincy/webhook-ingress` passed 1 file / 13 tests.
- `@quincy/app` and `@quincy/background` could not start their Wrangler suites in this sandbox: both failed before test collection with `listen EPERM: operation not permitted 127.0.0.1`, alongside pre-existing Wrangler log-file `EPERM` diagnostics for `/Users/tingruilee/Library/Preferences/.wrangler/logs`. Their suites require the orchestrating session's outside-sandbox re-verification.

`npx vitest run --config packages/shared/vitest.config.ts`

- PASS — 9 files, 60 tests. This is the mandatory separate shared-package suite because the workspace command does not own it.

## Exact source/build audits

Commands from the plan were run from `portal/`.

- Direct dependency: exactly one `@tanstack/react-query` pin at `portal/package.json` and the matching root lockfile entry: `5.102.3`; `npm ls` reports `@tanstack/query-core@5.102.3` only as its expected transitive dependency.
- No `react-router`, `@tanstack/react-router`, `WebSocket`, or `EventSource` match in the audited package/source paths.
- `refetchInterval` appears only in the two migrated ordinary query hooks and the passive RAW observer; ordinary visible interval is exactly `30_000` ms and no global interval exists.
- `setInterval`/`setTimeout` audit shows the existing UploadDropzone 5-second publication poll, existing ProjectWorkspace active-job 5-second interval, existing legacy AutoHDR 5-second recursive timeout, and the bounded 2.5-second Dropbox cycle. No additional 5-second timer was introduced.
- `git diff --check`: PASS.

## Round-2 count delta

- Before this round: focused TB2 seam 2 files / 18 tests and 3 DOM files / 45 tests; full web 13
  node files / 86 tests and 21 DOM files / 210 tests.
- After this round: focused TB2 seam 2 files / 19 tests and 3 DOM files / 49 tests; full web 13
  node files / 88 tests and 22 DOM files / 216 tests.
