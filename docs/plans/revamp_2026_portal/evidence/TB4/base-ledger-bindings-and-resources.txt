TB4 base / ledger / resource preflight (2026-08-26)

Base commit:
  bb37ff0ea67fe971cfbba5a1d8501c6060346bee (main; worktree was clean before TB4 edits)

Checked-in local preflight:
  - Migration files ended at 0030_project_comment_read_markers.sql before this change.
  - No notification_outbox or notification_delivery_ledger table/index existed before this change.
  - Existing Wrangler configs contained quincy-ingest, quincy-renditions, and
    quincy-renditions-dlq; they contained no quincy-notifications or
    quincy-notifications-dlq binding before this change.
  - 0031 is now the only new migration and the two Queue names are used only by the TB4
    producer/consumer configuration.

Live/remote inventory disposition:
  The plan's remote D1 migration-ledger and Cloudflare Queue inventory checks were not run.
  The task's hard exclusion explicitly forbids production interaction, including remote reads,
  and no remote D1/Queue command was attempted. Therefore this code-only session cannot claim
  that 0031 and both Queue names are genuinely free in the live account. Production preflight
  must confirm that before any deployment/provisioning authorization; if either is occupied, stop
  rather than renumbering or rebinding.

No sign-in, remote migration, Queue provisioning, Worker deployment, or production mutation was
performed.
