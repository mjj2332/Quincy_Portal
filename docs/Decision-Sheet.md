---
Status: Approved
Owner: mjj2332@gmail.com
Last updated: 2026-08-24
Approved on: 2026-07-19
Authority revision approved on: 2026-08-24
---

# Quincy Portal — Decision Sheet

> **Purpose:** One-pass approval sheet for product decisions that can change schema, auth, flow, or implementation order.
> **Recommended posture:** Approve the defaults unless there is a known operational reason to change them before coding starts.

## Blocking before coding

| ID | Decision | Options | **Recommended default** | Why | Affects | Source | Approve ☐ / Change: ___ |
|---|---|---|---|---|---|---|---|
| D-01 | What does "RAW" mean in the portal, and what upload formats are accepted? | A. Portal "RAW" = full-resolution JPEG exports from Lightroom only. B. True camera RAW + JPEG. C. Any image/RAW format. | **A — JPEG-only ingest.** Accept `.jpg` / `.jpeg` for photo ingest; camera RAW stays with the photographer as backup and is never uploaded. | This matches `Implementation-Proposal.md` and avoids RAW decoding, Cloudflare Images source limits, and unpredictable browser/backend processing. The UI can keep the label "RAW" if it means "unedited capture JPEGs awaiting QA." | schema / flow / UI | Implementation-Proposal §1/§6.4/§8 | Approve ☑ |
| D-02 | What can photographers access? | A. Assigned projects only, RAW-only. B. All projects, RAW-only. C. Assigned projects plus edited/client delivery preview. | **A — assigned projects only, RAW-only.** They can upload, view, annotate, compare, and recommend RAW JPEGs on assigned shoots; they can see basic project status and selected-for-editing state, but not Edited, publish, client links, or other projects. | This is the least-leaky model and aligns with capability-based auth. It still gives photographers enough feedback to confirm uploads and leave useful QA notes. | schema / auth / flow / UI | Personas §2; PRD §4; Implementation-Proposal §6.3 | Approve ☑ |
| D-03 | Should the client approval / guest-reviewer step exist in MVP? | A. Drop it; clients only receive final delivery. B. Keep a guest-reviewer link before final delivery. C. Merge review and delivery into the same client link. | **A — drop from MVP.** Build final delivery only; leave the data model able to add a future `review` link type if needed. | This removes a whole external workflow, auth state, notification path, and approval status from the first build. The internal Editor/QA publish gate already covers quality control. | schema / auth / flow / UI | Personas §4; PRD §8 | Approve ☑ |
| D-04 | What are default client link expiry and passcode rules? | A. 30-day expiry, passcode optional. B. No expiry, passcode optional. C. 14-day expiry, passcode required. | **A — 30-day default expiry; passcode optional per link.** Store link tokens hashed at rest, make links revocable, set `Referrer-Policy: no-referrer`, and require server-side unlock checks for premium/full-res paths. | This follows the signed-link hardening plan without making standard delivery too slow for agents. Passcodes stay available for sensitive jobs or agency preference. | schema / auth / flow / UI | PRD §6.7; Implementation-Proposal §6.3/§6.9 | Approve ☑ |
| D-05 | When Tonomo creates/updates a project, what start-state should it use? | A. Always create at `Awaiting RAW`. B. Attach any finished deliverables and skip ahead. C. Per-service hybrid. | **C — per-service hybrid.** Create the project at `Awaiting RAW` when photos are ordered; attach Tonomo-provided finished video, floorplan, or copy links immediately to their own collections with received/status metadata. | This preserves the capture pipeline while using the useful deliverable links already present in Tonomo. It also matches the proposal's per-service flag and reconciliation plan. | schema / flow / UI | PRD §4a; Implementation-Proposal §6.5 | Approve ☑ |
| D-06 | Are Admin and Project Manager one role or two? | A. One role for MVP. B. Two separate roles now. C. One visible role, separate hidden capabilities. | **A — one visible Admin/PM role for MVP, implemented through capabilities.** Internally model permissions as capabilities so the role can split later without rewriting checks. | A one-to-three person studio build does not need extra role UX up front. Capability-based authorization preserves the future option to split user/settings admin from shoot operations. | schema / auth / UI | Personas §1; PRD §3/§4; Implementation-Proposal §6.3 | Approve ☑ |
| D-07 | Should Edited QA support RAW-vs-Edited compare? | A. Yes in Phase 2. B. Edited-vs-Edited only. C. Defer until after delivery launch. | **A — yes in Phase 2.** Store source RAW asset linkage and matched web renditions so QA can compare the unedited JPEG export against the returned edit. | The implementation proposal already designs ingest renditions around this. It is a high-value QA workflow and should be solved before the internal capture-to-edit loop is considered complete. | schema / flow / UI | PRD §5/§8; Implementation-Proposal §6.4/§11 | Approve ☑ |
| D-08 | What is the floorplan upload shape? | A. PDF + JPG/PNG preview with immutable versions. B. PDF only. C. Free-form files. | **A — each floorplan version has a downloadable PDF and a gallery preview image.** Store revisions immutably with `version` / `supersedes_asset_id`; publish the latest approved version by default. | This gives clients a clean preview plus the production-ready file, while matching the R2 asset/version model. It avoids vague "misc file" handling that becomes hard to publish and replace safely. | schema / flow / UI | PRD §6.5; Implementation-Proposal §6.10 | Approve ☑ |
| D-09 | Should the prototype "View as" switcher stay while real role-gating is added? | A. Keep it as a non-production/demo affordance and layer real role-gating underneath. B. Remove it. C. Keep it in production for all users. | **A — keep only in dev/staging builds; never ship production impersonation.** Strip the "View as" affordance from production builds via build-time env. Production access always derives from the authenticated user's capabilities. | This preserves stakeholder demo and QA speed in non-production contexts without creating a production impersonation path. Capability checks remain the source of truth for every production route and UI surface. | auth / UI | Sitemap §Prototype "View as"; PRD §4; Implementation-Proposal §6.3 | Approve ☑ |
| D-14 | Confirm staff authentication method. | A. Cloudflare Access for staff; clients use signed no-login links. B. Build custom staff login. C. Use client accounts for all users. | **D — Custom: Google Account Login + Magic Link Email planned for staff (revised 2026-07-19); clients use signed no-login links.** Both methods stay in the approved design, but **only Google OAuth ships in MVP**. Magic link is built behind a `sendEmail()` seam and implemented later, only when a real need arises (e.g. a staff member without a Google account) — not on a fixed phase date. User/session records in D1 via better-auth. Closed system — Admin provisions the user row first; sign-in only succeeds for allow-listed emails. Clients remain behind signed token checks. | Deferring magic link removes the transactional-email dependency and login single-point-of-failure from MVP without giving up the option — the seam means adding it later needs no rework. Requires every current staff member to have a Google account for now. | auth / infra / flow | Implementation-Proposal §1/§6.3; Implementation-Plan §2 A1 | Change: ✓ Google + Magic Link planned; Google ships first, magic link deferred until needed |
| D-15 | Confirm frontend stack. | A. React 18 + TypeScript + Vite SPA with Quincy design system and Hono RPC. B. React Router SSR by default. C. Different frontend framework. | **D — Custom: latest stable pinned React 19.2 patch + TypeScript + Vite SPA reusing the Quincy design system, internal API via Hono RPC (revised 2026-08-24).** Revise only the React/React DOM runtime major through TB0A; retain the rest of the approved stack. | A standalone compatibility release modernizes the supported React runtime without authorizing a framework/router migration, SSR, Server Components, React Compiler, or product redesign as collateral work. | UI / architecture / API / runtime | Implementation-Proposal §1/§6.1/§6.2; Quincy Portal Revamp Brief §Authority proposal; Implementation-Plan §2 A12 | Change: ✓ React 19.2 patch only; TypeScript/Vite SPA/Quincy/Hono retained |
| D-16 | What UI platform and convergence contract should govern the revamp? | A. Tailwind CSS v4 plus source-owned shadcn components under Quincy visual authority. B. Keep every surface on unconstrained hand-authored CSS. C. Replace the SPA/design system wholesale. | **A — adopt Tailwind CSS v4 and source-owned shadcn incrementally only after TB0A and TB0B are live.** Use Base UI/Sera/Lucide, semantic Quincy tokens, ~~controlled/disabled~~ **Preflight enabled (revised 2026-09-04, TB8-10B §5/D-07)**, no dark mode, and fixed matched evidence at 1440×900, 1024×768, and 390×844. FullCalendar's official shadcn registry is one later reviewed specialist exception, not visual authority. | This creates a maintainable component platform while preserving Quincy's fonts, geometry, signals, accessibility, working product behavior, and evidence-led convergence instead of shipping a stock framework aesthetic or a rewrite. Preflight was revisited once `app.css` stopped carrying a `.button` family (D-06): an audit of every rule in Tailwind's Preflight stylesheet found the app's unlayered CSS already wins the cascade almost everywhere, with a handful of confirmed gaps (rich-text list markers, several bare calendar form controls, inherited letter-spacing) — see `docs/plans/TB8-10B-Dead-CSS-Sweep-And-Button-Retirement-Plan.md` §5/§11 for the full audit, Sol's review, and the computed-style verification. | UI / accessibility / architecture / delivery sequence | Quincy Portal Revamp Brief §Program outcomes; revamp package core/11 | Approve ☑ |
| D-17 | What architecture should govern freshness, collaboration, notifications, pipeline semantics, and Kanban? | A. Preserve deep routes and Quincy-owned Cloudflare data while modernizing each boundary incrementally. B. Replace routing/state/collaboration wholesale with an external platform. C. Leave current freshness and ordering behavior unchanged. | **A — keep the typed custom router and adopt TanStack Query incrementally; keep discussions, read state, activity, and notification delivery Quincy-owned on Cloudflare.** Use route/resource/range-aware query identities, bounded refresh and draft/interaction preservation; a D1 outbox + Cloudflare Queue with send-time authorization; fixed semantic Stage identities; developer-managed global Stage order; and `boardPosition` as the sole persisted manual Kanban order before dnd-kit interaction modernization. | The current product already has valuable routes, comments, notifications, AutoHDR/job refresh, and Kanban behavior. Incremental ownership changes preserve those contracts, prevent cross-project/access leakage and duplicate semantic events, and avoid a rewrite. | UI / data lifecycle / notifications / pipeline / Kanban / Cloudflare | Quincy Portal Revamp Brief §§Program outcomes, Data/discussion/notifications; revamp package core/05–08 | Approve ☑ |
| D-18 | Where should project coordination live, and how are Stage, Deadline, team, and Editor-wide changes governed? | A. Make the Project Workspace left rail canonical. B. Keep project-level controls duplicated in Collaboration/Edit Project. C. Create a separate coordination product. | **A — make the Project Workspace left rail canonical for Stage, project Deadline/reminders, Photographers, and Editors; keep Collaboration focused on checklist/subtasks and project discussion.** Retain `editProject` for roster and Deadline writes; introduce `moveProjectStage` for every Stage movement path; use Sydney civil-time/version rules for Deadline reminders; and deliver approved role-safe project changes to eligible assigned Editors through the durable registry. | One canonical operational surface prevents conflicting ownership while preserving task-level collaboration. Shared guarded commands, membership cycles, Deadline revisions, and finite notification rules keep rail, Kanban, and delivery behavior coherent. | flow / UI / auth / schema / notifications / Kanban | Quincy Portal Revamp Brief §§Canonical coordination model, Project Deadline; revamp package core/03, core/07, core/08 | Approve ☑ |
| D-19 | How should non-employee External Editors access Quincy production work? | A. Add a distinct assignment-scoped global role with a shared safe projection. B. Reuse the internal Editor role and broad access. C. Create a separate portal/data copy. | **A — add global role `external_editor`, displayed External editor, while retaining existing project membership role `editor`.** Require current assignment for every project surface; grant only the explicit approved production/collaboration capabilities; withhold broad project, Admin, Notice Board, delivery/publish, RAW-selection, extras, and AutoHDR administration; and enforce one shared server-side external-safe projection, role/session lifecycle, project-scoped participant contact, and assigned-scope Calendar. | A distinct least-privilege role lets contractors work across assigned productions without exposing unrelated projects, internal notes/contact/billing/Dropbox/provider data, staff-wide surfaces, or Admin operations. | auth / privacy / schema / API / UI / notifications / Calendar | Quincy Portal Revamp Brief §External Editor; revamp package core/13 | Approve ☑ |

## Can defer

| ID | Decision | Options | **Recommended default** | Why | Affects | Source | Approve ☐ / Change: ___ |
|---|---|---|---|---|---|---|---|
| D-10 | When should native video hosting replace Vimeo? | A. Vimeo now; Cloudflare Stream later. B. Stream in MVP. C. Direct R2 video files. | **A — Vimeo now, Stream later.** Keep Vimeo links through the internal and first client-delivery build. | The proposal intentionally defers Stream because video hosting is not the riskiest workflow. Signed Stream playback can be added when paywall or native upload needs justify it. | schema / flow / UI | PRD §6.4; Implementation-Proposal §1/§9 | Approve ☑ |
| D-11 | Should photographers get a filtered dashboard or a stripped-down uploads screen? | A. Filtered dashboard. B. Upload-only screen. C. Both. | **A — filtered dashboard first.** Show assigned shoots only, with RAW-focused actions and read-only stage context. | This reuses the project dashboard while enforcing D-02. A specialized upload screen can come later if field use shows the dashboard is too much. | flow / UI | Personas §2; PRD §6.1; Sitemap §Screen-by-screen routing notes | Approve ☑ |
| D-12 | Who creates copywriting, and should copy be editable in-portal? | A. PDF upload only now. B. In-portal copy editor now. C. External copywriter role now. | **A — PDF upload only now.** Admin/Editor can upload and publish a copywriting PDF; in-portal text and social content are later. | This matches the current PRD and avoids building an editorial CMS before the capture pipeline is stable. | schema / flow / UI | PRD §6.6; Personas §3; Implementation-Proposal §6.10 | Approve ☑ |
| D-13 | What happens to Clients and Schedule nav stubs? | A. Leave hidden or stubbed until needed. B. Build both in MVP. C. Remove permanently. | **D — Custom: Clients remain deferred; authorize Production Calendar and checklist scheduling (revised 2026-08-24).** Add Calendar as a third Dashboard view after its prerequisite tracer bullets, backed by the approved project Deadline and additive checklist due/range schedule contracts. | Production scheduling is now an approved operational need, while a Clients management surface still does not block the production pipeline. Keeping the two nav decisions coupled under the old blanket deferral would hide that distinction. | flow / UI / schema / API | Sitemap §Top-level structure/§Screen-by-screen routing notes; PRD §9; Quincy Portal Revamp Brief §Authority proposal; Implementation-Plan §2 A13 | Change: ✓ Clients remain deferred; Schedule becomes the approved Production Calendar program |

Decisions D-01–D-15 approved on 2026-07-19 by mjj2332@gmail.com.
D-13 and D-15 revised; D-16–D-19 approved on 2026-08-24 by mjj2332@gmail.com.
D-16 revised (Preflight enabled) on 2026-09-04 by mjj2332@gmail.com — TB8-10B §5/D-07.
